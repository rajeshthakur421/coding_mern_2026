//a pure function is a function that always produces the same output for the same input and has no side effects.
//Pure functions do not modify variables outside their scope, 
function add(a, b) {
    return a + b;
  }
   
  console.log(add(2, 3)); // Output: 5